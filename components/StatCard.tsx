"use client";

// import { BarChartPlaceholder, LineChartPlaceholder } from "@/components/Charts";

// type StatCardProps = {
//   title: string;
//   value: string;
//   color: string;
//   type: "bar" | "line";
// };

// const StatCard = ({ title, value, color, type }: StatCardProps) => (
//   <div className="p-6 bg-white shadow-md text-center border-t-4" style={{ borderColor: color }}>
//     <h3 className="text-gray-500">{title}</h3>
//     <p className="text-xl font-semibold">{value}</p>
//     <div className="w-full h-24">
//       {type === "bar" ? <BarChartPlaceholder color={color} /> : <LineChartPlaceholder color={color} />}
//     </div>
//   </div>
// );

// export default StatCard;
